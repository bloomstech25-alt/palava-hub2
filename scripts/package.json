{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "grant-admin": "tsx ./src/grant-admin-claim.ts",
    "seed-schools": "tsx ./src/seed-schools.ts",
    "backfill-school-counts": "tsx ./src/backfill-school-counts.ts",
    "wipe-all-users": "tsx ./src/wipe-all-users.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:rules": "PATH=\"$(ls -d /nix/store/*-openjdk-21*/bin 2>/dev/null | head -1):$PATH\" firebase --config ../firebase.json --project palava-hub-rules-test emulators:exec --only firestore 'tsx ./src/test-firestore-rules.ts'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/node": "catalog:",
    "firebase": "^12.12.0",
    "firebase-admin": "^13.8.0",
    "firebase-tools": "^15.15.0",
    "tsx": "catalog:"
  }
}
